# Exiba os indices de cada item da lista ao lado do nome de cada item da lista sem usar a função enumerate

lista = ['Maria', 'Helena', 'Luiz', 'Pedrow']
for each_item in range(len(lista)):
    print(each_item, lista[each_item])

